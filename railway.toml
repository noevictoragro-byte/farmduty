[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build:server && npm run prisma:generate"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
NODE_ENV = "production"
